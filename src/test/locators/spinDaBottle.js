import { androidLocatorPath } from '../../utils/constant.js';
export const spinDaBottle = {
  android: {
    upBtn: androidLocatorPath + 'upBtn"]',
  },

  wap: {
    upBtn:
      './/*[not(contains(@class,"row-bet-place-disabled"))]/div/*[@data-cms-key="up"]',
  },
};
